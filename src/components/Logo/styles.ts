import styled from 'styled-components'

type LogoProps = { 
   
}

export const LogoElement = styled.h1<LogoProps>`
    font-size: 1.7rem;
    
   
    span { 
        margin-left: 0.2rem;
        color: var(--green900);
    }
`